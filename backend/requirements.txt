# JanVedha AI — Backend Dependencies (Vercel-Optimized)
# Python 3.11+

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
pydantic-settings
pydantic[email]
python-multipart

# ── MongoDB ───────────────────────────────────────────────────────────────────
motor>=3.3.0
beanie>=1.25.0

# ── AI / LLM / LangChain ──────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-google-genai>=1.0.0
langchain-community>=0.2.0
openai>=1.0.0              # Used for Sarvam-M via NVIDIA NIM API (API-based, very light)

# ── ML / Priority Model (Lightweight Fallback) ────────────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0
joblib>=1.3.0
# Removed lightgbm/shap/prophet/transformers/torch for Vercel size limits

# ── NLP / Language Detection ──────────────────────────────────────────────────
# langdetect removed as it lacks pre-built wheels and fails Vercel's --no-build flag.

bcrypt>=4.0.1
python-jose[cryptography]
PyJWT==2.8.0

# ── Caching / Sessions ────────────────────────────────────────────────────────
redis==5.0.3

# ── Notifications ─────────────────────────────────────────────────────────────
twilio
sendgrid
python-telegram-bot

# ── Task Queue ────────────────────────────────────────────────────────────────
celery==5.3.6

# ── Storage ───────────────────────────────────────────────────────────────────
aiofiles

# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx
beautifulsoup4

# ── Documents / Templates ─────────────────────────────────────────────────────
jinja2
# weasyprint removed (requires system-level Pango/GObject libraries not on Vercel)
